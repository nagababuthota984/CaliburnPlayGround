using System.Windows;

namespace CaliburnPlayGround
{
    /// <summary>
    /// Interaction logic for App.xaml
    /// </summary>
    public partial class App : Application
    {
        public App()
        {
            //way to start the bootstrapper without the help of app.xaml resources

            //Bootstrapper b = new();
            //b.Initialize();
        }
    }
}
